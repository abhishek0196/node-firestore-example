class ToDo {
    constructor(id, title, isCompleted, createdAt, updatedAt ) {
            this.id = id;
            this.title = title;
            this.isCompleted = isCompleted;
            this.createdAt = createdAt;
            this.updatedAt = updatedAt
    }
}

module.exports = ToDo;